const navlist = document.querySelectorAll('.nav__ul li');


// document.addEventListener('scroll', () => {
//     const clientPos = document.getBoundingClientRect();
//     if (condition) {
        
//     }
// })


// navlist.forEach((list) => {
// 	list.addEventListener('mouseover', () => {
// 		let position = list.getBoundingClientRect();
	
// 		overlay.style.left = (position.x - 20) + 'px';
	
// 	});
// 	list.addEventListener('mouseout', () => {
// 		overlay.style.left = (initPosition.x - 20) + 'px';
// 	});
// });

